# --- Безопасность --- #
# Никогда не добавляйте в образ приватные ключи и переменные окружения!
.env
.env.*
binance_keys/

# --- Контроль версий --- #
# Исключаем всю историю Git и воркфлоу GitHub
.git/
.github/
.gitignore

# --- Виртуальные окружения --- #
# Исключаем любые venv, чтобы не смешивать с системными библиотеками в образе
venv/
.venv/
env/

# --- Кэш и артефакты Python --- #
# Стандартные файлы кэша, которые не нужны в production
__pycache__/
*.pyc
*.pyo
*.pyd

# --- Конфигурации IDE и локальные настройки --- #
# Исключаем настройки редакторов и IDE
.idea/
.vscode/
.run/
*.run.xml

# --- Тестирование --- #
# Тесты и их артефакты не нужны в production-образе
tests/
.pytest_cache/
htmlcov/
.coverage

# --- Документация и проектные файлы --- #
# Вся документация не нужна для работы приложения
project_management/
*.md
*.puml
*.svg

# --- Данные runtime --- #
# JSON файлы репозиториев (не нужны в образе)
deals.json
orders.json
tickers.json
tickers_dump.json

# База данных (не должна быть в образе)
data/
*.db
*.sqlite
*.sqlite3

# Backup файлы и экспорт данных
*.backup
backup/
backups/
exports/
export_*.json

# --- Временные и системные файлы --- #
# Логи, временные файлы и системный мусор
*.log
order_book_analysis.log
*.tmp
*.swp
*.swo
.DS_Store
Thumbs.db

# --- Скрипты и утилиты для разработки --- #
# Песочницы и тестовые скрипты не должны быть в образе
sandbox*.py
test_prod.py

# --- Файлы сборки Docker --- #
# Сам Dockerfile и docker-compose не должны копироваться внутрь образа
Dockerfile
docker-compose.yml
.dockerignore