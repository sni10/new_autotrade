[pytest]
pythonpath = src
testpaths = tests
addopts =
    --verbose
    --tb=short
    --strict-markers
    --asyncio-mode=auto
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
filterwarnings =
    ignore::pytest.PytestReturnNotNoneWarning
